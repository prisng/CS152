object session {
  println("Welcome to the Scala worksheet")

  /********** #1 **********/
  // generic compose combinator
  
  /*
	def compose[A, B](f: A => B, g: B => A): B => B = {
		def r(x: B):
			B = f(g(x));
   	r _
	}
	*/
	
	// input of f should be same as output of g
	def compose[A, B, C](f: B => C, g: A => B): A => C = {
		def r(x: A):
			f(g(x))
   	r _
	}
	
	def triple(x: Int): Int = 3 * x
	
	def square(x: Int) = x * x
	
	val squareTriple = compose(triple _, square _)
	
	triple(2)
	square(triple(2))
	squareTriple(2)
	
  
  /********** #2 **********/
  
  
  /********** #3 **********/
  
  
  /********** #4 **********/
  
  
  /********** #5 **********/
  
  
}